{
  "name": "serverless-ssr-app",
  "private": true,
  "type": "module",
  "overrides": {
    "fast-xml-parser": "^5.3.4"
  },
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "build:lambda": "NITRO_PRESET=aws-lambda npm run build",
    "test:handlers": "node --test tests/api.test.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1126.0",
    "@aws-sdk/lib-dynamodb": "^3.1126.0",
    "nuxt": "^3.21.11",
    "vue": "^3.5.42",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
